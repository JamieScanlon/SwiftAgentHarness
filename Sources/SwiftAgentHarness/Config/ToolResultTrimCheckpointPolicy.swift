import Foundation

/// Fingerprint for persisted tool-result trim checkpoints (bump when trim rules change materially).
enum ToolResultTrimCheckpointPolicy {
    static let configFingerprint = "tool_result_trim_v1"
}
